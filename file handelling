#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream fout("data.txt");

    fout << "Hello File Handling!";
    fout.close();

    ifstream fin("data.txt");
    string line;

    getline(fin, line);
    cout << line;

    fin.close();

    return 0;
}
